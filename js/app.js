$(".carousel").carousel({
  interval: 3000, // Tempo entre as transições (em milissegundos)
  pause: "hover", // Pausa a transição quando o mouse está sobre o slide
  ride: "carousel", // Inicia automaticamente
});
